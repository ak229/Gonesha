package utils

// Application
const NAME string = "Gonesha"
const VERSION string = "0.0.1"

// Shell
const PROMPT string = "gonesha"
const TERMINATOR string = ">"

// Commands
const CLUSTER string = `cluster gives the information regarding the nodes in the cluster
and the modules installed in them`
const RUN string = `run is used to run a command on a particular host
Usage : run <ip | tag> <command>`
const STATUS string = `status gives the information regarding the status of the nodes in the cluster
along with the status of the modules installed in them`
